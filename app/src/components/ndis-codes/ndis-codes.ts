/* This module moved to @/lib/ndis-codes so that pure logic and data code
 * has no dependency on component code. Re-exported here so existing
 * component imports of "@/components/ndis-codes/ndis-codes" keep working. */
export * from "@/lib/ndis-codes";
